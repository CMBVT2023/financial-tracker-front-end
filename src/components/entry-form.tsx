import LabeledInput from "./labeled-input";

export default function EntryForm() {

    return (
        <form > 
            <LabeledInput inputType="text" labelText={"Item Name"} inputRef={}/>
        </form>
    )
}